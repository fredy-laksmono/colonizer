const Planet = ({player, gameState, updateGameState, x, y}) => {
    let toRender = (<div>
        <button>
            <div>Type</div>
            <div>Owner</div>
            <div>Units</div>
        </button>
    </div>);
    return toRender;
}

export default Planet;