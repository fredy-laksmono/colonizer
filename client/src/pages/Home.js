const Home = () => {
  let createRoomRender = (
    <div>
      <button>Create Game</button>
    </div>
  );
  let toRender = <div>{createRoomRender}</div>;
  return toRender;
};

export default Home;
